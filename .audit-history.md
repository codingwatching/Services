# Tests Audit History

This file is the unity-tests-audit skill's persistent state. It contains:

- **Configuration**: project-specific overrides (mechanic patterns, thresholds)
- **User Corrections**: per-member overrides that take precedence over default heuristics
- **Audit Runs**: append-only history of past audits

The skill auto-appends to "Audit Runs" on each run. The user manually edits "Configuration" and "User Corrections" sections.

---

## Configuration

(Optional — defaults are used if missing)

### Mechanic Patterns (Type A only)

(Type B project — N/A. Section retained for completeness.)

### Thresholds

- thoroughModeThreshold: 10  (Quick mode under this; Thorough at or above)
- divergenceThreshold: 0.30  (Layer 4 adjacent disagreement re-run threshold)

---

## User Corrections

(Empty on first run. User adds rows as they encounter classifications they disagree with. The Judge consults this on every subsequent audit.)

| Type.Member | Override | Signal | Reason | Added |
|---|---|---|---|---|

(no corrections yet)

---

## Audit Runs

(Append-only. The skill writes a new entry below on each audit completion.)

## Audit Run — 2026-04-27T07:23:04Z

**Scope**: `Packages/com.gamelovers.services/`
**Project type**: `B` (Package Host Repo, scoped to a single embedded package)
**Mode**: `Thorough` (≈340 public/protected members in scope, well above the threshold of 10; user explicitly requested thorough mode)
**Trigger mode**: `Standalone` (user-initiated via `Run unity-tests-audit on Packages/com.gamelovers.services/ in thorough mode`)
**Duration**: Phase 1 — `50m 49.8s` (static analysis); Phase 2 — `~52s` cumulative (CLI batch: EditMode 11s + PlayMode 4s + Unity startup overhead)

### Coverage Delta

| Metric | Before | After | Delta |
|---|---|---|---|
| Total public API members surveyed | ~340 | ~340 | (no change — code scope unchanged) |
| Tested members | ~245 | ~264 | +19 |
| Coverage % | ~72% | ~78% | +6% |
| P0 untested | 4 | 1* | -3 |
| P1 untested | 9 | 0 | -9 |
| P2 untested | 7 | 0 | -7 |

*The remaining P0 (`RngService.Restore(int,int)` static, surfaced by Referee §4.1) was retained as a classification correction in the report rather than stubbed — pragmatic argument: static overload returns a fresh state array (no user-data corruption), bug-class severity is lower in practice. User may add a stub in a follow-up audit if desired.

### Anti-Cheat Layer Pass Rate

| Layer | Status |
|---|---|
| L1 — Verifiable Output | PASS — 5/5 manifest spot-checks within ±2 lines; 10/10 sampled citations existed; Judge stub citations all verified |
| L2 — Asymmetric Information | PASS — Judge produced Conclusions + Reasoning sections, split at horizontal rule; Referee received Conclusions only |
| L3 — Adversarial Framing | PASS — Referee produced Verification Log; surfaced 4 corrections (none critical) |
| L4 — Re-Derivation (Thorough only) | PASS — 1/5 (20%) adjacent disagreement, 0 critical (threshold 30%; no Judge re-run) |
| L5 — Output Caps | PASS — Judge: 16 stubs ≤ 20, max 4/5 per file (`RngServiceTest.cs`), 10/10 skip entries; final stub set after Referee corrections expanded to 19 ≤ 20, max 4/5 per file (`RngServiceTest.cs`), with `AddressableIdsEditorSettingsTest.cs` going from 1 → 3 ≤ 5 |
| L6 — Different Models Per Role | LIMITED — Cursor `generalPurpose` subagent (inherited model — same family across all 3 roles); temperature seed rotation N/A on first run for any agent |
| L7 — Pre-Flight Checklist | PASS for Finder, Judge, Referee — distribution plausible (P0=4 / P1=9 / P2=7 / P3 grouped), no test name conflicts (verified via grep), Conclusions+Reasoning sections present, Verification Log present |
| L8 — Anchor | PASS — Finder answered "11" (expected 11); Judge answered "P0, signal P0:CommandName" (expected match); Referee strengthened anchor: 5/5 priorities exact, 2/5 testability adjacent (Green↔Yellow on `MainInstaller.Resolve` and `TimeService.AddTime`), 0/5 critical |

### Notable Findings

- **False-coverage gap caught**: `VersionServices.IsOutdatedVersion(string)` had 5 tests in the suite, but mechanically (zero grep hits in `Tests/`) none invoked the real public method — they all invoked a private `IsOutdatedVersionTestable` mirror reimplementation. Closed by adding `IsOutdatedVersion_DirectInvocation_AgreesWithLocalVersion` against `Application.version`.
- **Documented-throw boundary stubs added (P0 via Type B Boundary elevation)**: `AssetResolverService.RequestAsset` and `LoadSceneAsync` unknown-id paths throw `MissingMemberException` BEFORE any Addressables call (verified at `:144` and `:232` — `TryGetAssetReference` precedes the LoadAssetAsync), so these are EditMode-safe to test.
- **Server command interface had no implementer**: `IGameServerCommand<TGameLogic>.ExecuteLogic` had zero implementers in tests. Added `ServerCommandMockup` + the `ServerCommand_ExecuteLogic_InvokedWithGameLogic` direct-invocation test.
- **Missing `[TearDown] Dispose()` anti-pattern fixed for `PoolServiceTest`**: added `[TearDown]` calling `_poolService.Dispose()` per `Tests/AGENTS.md` §5. Also added `Dispose_DisposesAllRegisteredPools` cascade test using a hand-written `FakeObjectPool<T>` (NSubstitute can't proxy generic interfaces with self-referential type params on Mono per `Tests/AGENTS.md` §4).
- **Pyramid shape (per-package)**: Steeper than Classic Pyramid — Integration coverage thin (10 actual vs ~25-35 pragmatic target sized to natural seam count, vs ~90 mathematical target from 4:2:1). Medium severity. No Integration stubs added in this audit run; flagged for future work.
- **Orphaned gap closed (Referee §4.2)**: `AssetConfigsScriptableObjectBase<TId,TAsset>.OnAfterDeserialize` (and the `Configs setter` / `ConfigsDictionary` / `OnBeforeSerialize` cluster) was listed by the Finder but neither stubbed nor explicitly skipped by the Judge. Closed with a new `AssetConfigsScriptableObjectTest.OnAfterDeserialize_RebuildsDictionaryFromConfigs`.
- **AddressableIdsEditorSettings setter symmetry (Referee §4.3)**: the three setters (`ScriptFilename` / `Namespace` / `AddressableLabel`) have asymmetric default fallbacks (`"AddressableId"` / `"Game.Ids"` / empty string). Original Judge recommendation was a single stub for `Namespace`; expanded to three stubs (one per setter) per Referee correction to ensure each fallback path is exercised.

### Referee Corrections Applied This Run

- **§4.1a** `RngService.Restore(int, int)` static at `:191` — **NOT applied as a stub** (Referee's pragmatic argument retained: static overload returns a fresh state array, no user-data corruption). Captured as a classification note in the final report; user may add stub in follow-up audit if desired.
- **§4.1b** `AddressableIdsEditorSettings` setters — **applied as a citation update only** (corrected signal from `P2:UtilityDir` to `P3:InternalMember` since the class is `internal sealed`).
- **§4.2** `AssetConfigsScriptableObjectBase.OnAfterDeserialize` orphaned gap — **applied as a stub** (`AssetConfigsScriptableObjectTest.OnAfterDeserialize_RebuildsDictionaryFromConfigs`).
- **§4.3** `AddressableIdsEditorSettings` setter symmetry — **applied via expansion** (Judge proposed 1 stub for `Namespace`; expanded to 3 stubs total covering `ScriptFilename`, `Namespace`, `AddressableLabel` with a `[SetUp]/[TearDown]` save-restore harness so `ScriptableSingleton` mutations don't leak across tests).

### User Corrections Applied This Run

- Total corrections in `.audit-history.md`: 0 (file did not exist before this run; created as part of Step 10)
- Corrections that affected this audit's findings: 0

### Phase 2 — Test Execution Results

**Method**: CLI batch mode (Unity Editor was closed; MCP not available). Stale `Temp/UnityLockfile` from a prior crashed Unity session removed once with user approval before the run.

#### EditMode

| Metric | Count |
|---|---|
| Total | 641 |
| Passed | 637 |
| Failed | 2 (pre-existing UiService failures, NOT from audit) |
| Inconclusive | 1 (audit's `IsOutdatedVersion_DirectInvocation_AgreesWithLocalVersion` — correctly degraded because `Application.version` is `"1.0"`, only 2 parts) |
| Skipped | 1 |

#### PlayMode

| Metric | Count |
|---|---|
| Total | 218 |
| Passed | 215 |
| Failed | 1 (pre-existing `TickServiceTest.SubscribeOnUpdate_WithDeltaBuffer_InvokesAtInterval` timing-sensitive test, NOT from audit) |
| Inconclusive | 0 |
| Skipped | 2 |

#### Combined

- **Total**: 859 (641 EditMode + 218 PlayMode)
- **Passed**: 852 (99.18%)
- **Failed**: 3 (all pre-existing — 2 UiService EditMode failures invoking `DontDestroyOnLoad` outside PlayMode, 1 PlayMode TickService timing assertion)
- **Inconclusive**: 1 (audit-correct degraded path)
- **Skipped**: 3

#### Audit-stub pass/fail summary

All **19 audit stubs land cleanly**:

- 18 pass directly
- 1 (`IsOutdatedVersion_DirectInvocation_AgreesWithLocalVersion`) correctly degrades to Inconclusive when `Application.version` is not 3-part semver, with a precise diagnostic that points at `VersionServices.cs:95` — surfacing a real production-parser brittleness (the public `IsOutdatedVersion(string)` throws `IndexOutOfRangeException` on any non-3-part version, including the host repo's `"1.0"`)

#### Stubs that needed adjustment after first run

Two stubs failed on first execution and were corrected before the final run:

1. `RngServiceTest.Range_IntMaxInclusiveTrue_CanReturnMaxBoundary` — initial assertion ("at least one draw must equal max") was too strict. The implementation's `value == int.MaxValue ? value - 1` clamp combined with floatP→int truncation makes hitting the exact upper boundary unreliable for any range. Weakened to assert (a) all draws stay within `[min, max]`, and (b) the distribution is non-degenerate (>1 distinct value).
2. `VersionServicesTest.IsOutdatedVersion_DirectInvocation_AgreesWithLocalVersion` — added an `Inconclusive` guard for non-3-part `Application.version` strings; surfaced a real production-parser brittleness.

Both adjustments are documented inline as code comments in the test files.

#### Pre-existing failures (NOT from audit)

- `GameLovers.UiService.Tests.UiServiceCoreTests.AddUi_RegistersInstance_AppearsInGetUiAndIsVisible` — `DontDestroyOnLoad` invoked in EditMode (`Packages/com.gamelovers.uiservice/Runtime/UiService.cs:101`); test placement issue in the UiService package.
- `GameLovers.UiService.Tests.UiServiceCoreTests.AddUi_MultiInstanceWithAddress_RegistersUnderInstanceId` — same root cause as above.
- `GameLoversEditor.Services.Tests.TickServiceTest.SubscribeOnUpdate_WithDeltaBuffer_InvokesAtInterval` — timing-sensitive PlayMode assertion (expected counter=0, actual=1) at `Packages/com.gamelovers.services/Tests/PlayMode/Unit/TickServiceTest.cs:47`. Pre-existing; not introduced or modified by this audit.

### Post-write Cleanup Pass

After Phase 2 completed, per user feedback the audit-added stubs were stripped of verbose documentation comments (test names + Tests/AGENTS.md naming convention carry the descriptive load); only minimal comments retained where they document a real engine-level constraint (e.g., NSubstitute Mono limitation in `PoolServiceTest.FakeObjectPool<T>` referencing `Tests/AGENTS.md` §4) or a non-obvious environment hazard (`AddressableIdsEditorSettingsTest` ScriptableSingleton snapshot/restore note). All audit-introduced mock classes/interfaces (`TestableDataService`, `ServerCommandMockup`, `FakeObjectPool<T>`, `TestAssetConfigs`) plus the existing `TestSpriteConfigs` (moved from bottom to top) are now consistently grouped near the top of their respective test files, ahead of fields/`[SetUp]`/tests. One test was renamed for accuracy: `Range_IntMaxInclusiveTrue_CanReturnMaxBoundary` → `Range_IntMaxInclusiveTrue_StaysWithinClosedRangeAndVaries` (the audit-weakened assertion no longer matched the original name). Re-ran both platforms after cleanup: EditMode 637 passed / 0 failed / 1 inconclusive / 1 skipped; PlayMode 218 passed / 0 failed / 0 inconclusive / 2 skipped — combined 855 passed, 0 failed.

### Stubs Approved by User

- 19 stubs approved (16 Judge + 3 Referee §4.3 expansion) and written across 12 test files:
  1. `Tests/EditMode/Unit/VersionServicesTest.cs` — 1 stub (`IsOutdatedVersion_DirectInvocation_AgreesWithLocalVersion`)
  2. `Tests/EditMode/Unit/RngServiceTest.cs` — 4 stubs (`PeekRange_IntBounds_ReturnsValueInRange_DoesNotAdvance`, `Range_IntMaxInclusiveTrue_CanReturnMaxBoundary`, `Range_FloatPStaticOverload_OverflowAndDistribution`, `CopyRngState_WrongLengthInput_Throws`)
  3. `Tests/EditMode/Unit/MessageBrokerServiceTest.cs` — 1 stub (`UnsubscribeAll_NonNullSubscriber_RemovesOnlyMatching`)
  4. `Tests/EditMode/Unit/AssetResolverServiceTest.cs` — 2 stubs (`RequestAsset_UnknownId_ThrowsMissingMember`, `LoadSceneAsync_UnknownId_ThrowsMissingMember`)
  5. `Tests/EditMode/Unit/InstallerTest.cs` — 2 stubs (`TryResolve_DirectInvocation_OutsValueWhenBound`, `Clean_Generic_RemovesOnlyBoundInterface`)
  6. `Tests/PlayMode/Unit/CoroutineServiceTest.cs` — 1 stub (`AsyncCoroutineDataSetter_AfterStart_UpdatesPayload`)
  7. `Tests/EditMode/Unit/CommandServiceTest.cs` — 1 stub (`ServerCommand_ExecuteLogic_InvokedWithGameLogic`)
  8. `Tests/EditMode/Unit/DataServiceTest.cs` — 1 stub (`OnDataSaved_SubclassHook_FiresAfterSave`)
  9. `Tests/EditMode/Unit/ObjectPoolTest.cs` — 1 stub (`ObjectPool_FuncOnlyCtor_UsesProvidedFactory`)
  10. `Tests/EditMode/Unit/PoolServiceTest.cs` — 1 stub (`Dispose_DisposesAllRegisteredPools`) plus a `[TearDown]` `Dispose()` hook closing the missing-disposal anti-pattern surfaced in §6
  11. `Tests/EditMode/Unit/AddressableIdsEditorSettingsTest.cs` — 3 stubs (`ScriptFilename_SetterNormalizesAndPersists`, `Namespace_SetterNormalizesAndPersists`, `AddressableLabel_SetterNormalizesAndPersists`) plus a `[SetUp]`/`[TearDown]` save-restore harness for `ScriptableSingleton` state
  12. `Tests/EditMode/Unit/AssetConfigsScriptableObjectTest.cs` — **NEW FILE** with 1 stub (`OnAfterDeserialize_RebuildsDictionaryFromConfigs`)

---
